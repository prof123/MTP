#!/bin/bash

# read procfs every 2 seconds and store it. tab separated instead of otherwise?
# Put timestamps which are consistent with ftrace timestamps.
# trace on/off ?
#record other things like start time, wall-clock time.
# run perf if required?
# dmesg output?
